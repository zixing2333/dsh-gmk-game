/**
 * DSH Gomoku Game Plugin - Host entry
 * Pure client-side UI plugin: this empty apply ensures the plugin
 * appears in the host cordis.yml / Loader. The browser half ships
 * via exports["./client"].
 */
export function apply() {}
